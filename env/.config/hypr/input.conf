# Shared input behavior. Keyboard layout lives in custom_settings.conf.
input {
    follow_mouse = 1
    sensitivity = 0

    touchpad {
        natural_scroll = false
    }

    repeat_delay = 250
    repeat_rate = 30
}
