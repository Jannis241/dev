# Themes

Themes werden ohne Picker gewechselt:

```vim
:Theme vim
:Theme default
:Theme ayu
:Theme rose-pine-moon
```

`vim` ist das Vim-artige Default-Theme. Der Theme-Command speichert die Wahl in `stdpath("state") .. "/jannis-theme"`.
